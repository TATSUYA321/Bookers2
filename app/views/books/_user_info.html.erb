<% if book.errors.any? %>
  <%= book.errors.count %>error prohibited
  <%= book.errors.full_messages.each do |message| %>
  <%= message %>
  <% end %>
<% end %>



 <h2>User info</h2>
 <%= attachment_image_tag user, :profile_image,:fill, 70,70, format: 'jpeg',fallback: "no_image.jpg",size:'70x70' %>
 <table class= "table">
   <tbody>
     <tr></tr>
     <tr>
       <th>name</th>
       <th><%= user.name %></th>
     </tr>
     <tr>
       <th>Introduction</th>
       <th><%= user.introduction %></th>
     </tr>
   </tbody>
 </table>
   <%=link_to  '',edit_user_path(@user.id), class: "btn-block fas fa-user-cog btn btn-outline-secondary"%>

<h2>New book</h2>
<%=form_with model: book,  local: true do |f| %>
<h4>Title</h4>
<%=f.text_field :title %>
<h4>Opinion</h4>
<%=f.text_area :body %>
<%=f.submit 'Create Book',class: "btn btn-success" %>
<% end %>
